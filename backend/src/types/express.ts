export interface ParamsWithId {
  id: string;
}

export interface ParamsWithUserId {
  userId: string;
}

export interface ParamsWithPageId {
  pageId: string;
}

export interface ParamsWithSectionId {
  pageId: string;
  sectionId: string;
}

export interface ParamsWithWebsiteId {
  websiteId: string;
}

export interface ParamsWithPageAndWebsite {
  websiteId: string;
  pageId: string;
}